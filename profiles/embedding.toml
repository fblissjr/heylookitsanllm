[meta]
name = "embedding"
description = "For hidden states extraction, not generation"

[defaults]
max_tokens = 2048
cache_type = "standard"
temperature = 0.7
top_k = 50
top_p = 0.95
repetition_penalty = 1.0
