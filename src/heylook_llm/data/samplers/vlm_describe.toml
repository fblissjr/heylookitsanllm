[meta]
name = "vlm-describe"
description = "VLM-safe subset for image description. mlx-vlm ignores top_k/min_p/repetition_penalty in stream_generate, so those fields are deliberately omitted."

[defaults]
temperature = 0.7
top_p = 0.9
max_tokens = 1024
