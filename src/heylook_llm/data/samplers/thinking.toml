[meta]
name = "thinking"
description = "Anti-loop overlay for thinking mode: presence penalty only. Per-model decode tuning (temperature/top_p/top_k) comes from the model's own generation_config.json vendor layer, not from this sampler. Auto-applied when a request (or model config) enables thinking."

[defaults]
presence_penalty = 1.5
enable_thinking = true
