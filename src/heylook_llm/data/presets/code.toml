[meta]
name = "code"
description = "Tight sampling tuned for code generation and completion. Lower temperature than 'deterministic' with more headroom."

[defaults]
temperature = 0.3
top_p = 0.95
top_k = 40
min_p = 0.05
max_tokens = 4096
repetition_penalty = 1.02
